import { DragScrollItemDirective } from './drag-scroll-item.directive';

describe('DragScrollItemDirective', () => {
  it('should create an instance', () => {
    const directive = new DragScrollItemDirective();
    expect(directive).toBeTruthy();
  });
});
